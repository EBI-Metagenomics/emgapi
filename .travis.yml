language: python
python:
  - "3.5"
  - "3.6"
  - "nightly"

install:
  - pip install -r requirements.txt
  - pip install flake8 pycodestyle pep8-naming

script:
  - flake8 --version
  - flake8 -v .
  - python setup.py test